import {nav} from "./AppNav.module.css";     // ! instead of styles we also dectruct it but dont try descturcturing
 
function AppNav() {
    return (
        <nav className={nav}>
            App Navigation
        </nav>
    )
}

export default AppNav
